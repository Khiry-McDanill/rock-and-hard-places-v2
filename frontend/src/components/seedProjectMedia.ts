// Compatibility entry point; all removable media assignments live in one registry.
export { seedProjectMedia, resolveProjectMedia } from './seedMedia';
export type { ProjectDisplayContext, ProjectMediaFrame } from './seedMedia';
